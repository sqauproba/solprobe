# Changelog

## [Unreleased]

### Added
- `solprobe status` command with network overview table.
- `solprobe network health` command.
- `solprobe rpc benchmark` and `solprobe rpc compare` commands.
- `solprobe slots latest` and `solprobe slots watch` commands.
- `solprobe fees` command.
- `solprobe wallet inspect` / `wallet watch` commands.
- `solprobe program inspect` / `program logs` commands.
- `solprobe tx inspect` / `tx simulate` commands.
- `solprobe tui` terminal UI entrypoint.
- Pydantic settings model and CLI config loader.
