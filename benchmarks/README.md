# Benchmarks

Reproducible benchmark harnesses. Run these to generate your own results
rather than relying on hard-coded numbers.

- `rpc/` — RPC latency benchmarks (also exposed via `solprobe rpc benchmark`)
- `websocket/` — WebSocket reconnect and event-throughput benchmarks
- `transaction/` — transaction inspection benchmarks
